/** Cloud Functions deployment region */
export const REGION = "australia-southeast1";

/** Production domain for Stripe redirects */
export const DOMAIN = "https://ravecave.app";

/** Embedding vector dimensionality (must match Firestore vector index) */
export const EMBEDDING_DIMENSIONS = 768;

/** Embedding model ID */
export const EMBEDDING_MODEL = "gemini-embedding-001";
